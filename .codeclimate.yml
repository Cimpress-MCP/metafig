---
engines:
  csslint:
    enabled: false
  duplication:
    enabled: true
    config:
      languages:
      - javascript
  eslint:
    enabled: true
  fixme:
    enabled: true
  nodesecurity:
    enabled: false # we'll run locally so we don't need to shrinkwrap
ratings:
  paths:
  - "**.js"
exclude_paths:
- node_modules/
- test/
- ".serverless/"
- reports/
- ".git/"
